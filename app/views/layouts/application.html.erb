<!DOCTYPE html>
<html>
<head>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= content_for?(:title) ? yield(:title) : "My Application" %></title>
  <meta name="description" content="<%= content_for?(:description) ? yield(:description) : "My Application" %>">
  <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
  <!-- Custom Fonts -->
  <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/font-awesome/4.4.0/css/font-awesome.min.css">
  <link href="http://fonts.googleapis.com/css?family=Lato:300,400,700,300italic,400italic,700italic" rel="stylesheet" type="text/css">
</head>
<body>
  <header>
    <%= render 'layouts/navigation'%>
  </header>
  <div class="wrapper">

    <%= render 'layouts/messages' %>
    <%= yield %>
  </div>
</body>
<footer>
</footer>
</html>

<!-- /.modal compose message -->
<div class="modal fade" id="signin-modal" tabindex="-1" role="dialog">
  <div class="modal-dialog">
    <div class="modal-content">

      <div class="modal-header" style="border:none;">
        <button type="button" class="close" data-dismiss="modal" aria-hidden="true">×</button>
        <h3 style="text-align:center;">Sign In</h3>
      </div>

      <div class="modal-body" style="min-height:425px;">
        <%= simple_form_for(resource, :as => resource_name, :url => session_path(resource_name), :html => { :role => 'form'}) do |f| %>

        <div class="row">
          <%= devise_error_messages! %>
          <div class="col-md-6 col-md-offset-3" style="padding-bottom:10px;">
            <div class="form-group">
              <%= f.email_field :email, :autofocus => true, :placeholder => "Email", class: 'form-control  style-minimalistic' %>
            </div>
          </div>
          <div class="col-md-6 col-md-offset-3" style="padding-bottom:15px;">

            <div class="form-group">
              <%= f.password_field :password, :placeholder => "Password", class: 'form-control style-minimalistic' %>
            </div>
          </div>
          <div class="col-md-6 col-md-offset-3" style="text-align:center;padding:5px 0px;padding-bottom:30px;">
            <% if devise_mapping.rememberable? -%>
            <div class="checkbox" style="width:150px">
              <label>
                <%= f.check_box :remember_me %> Remember me
              </label>
            </div>
            <% end %>   
            <%- if devise_mapping.recoverable? %>
            <%= link_to "Forgot password?", new_password_path(resource_name), class: 'pull-right', style: "margin-top:-30px;padding-right:15px;" %>
            <% end %>           
          </div>


          <%= f.submit 'Sign in', :class => 'col-xs-8 col-xs-offset-2  col-sm-8 col-sm-offset-2 col-md-4 col-md-offset-4 btn btn-primary' %>
          <%- if devise_mapping.registerable? %>
          <div class="col-xs-12 col-sm-12 col-md-12" style="text-align:center;padding:5px 0px;">or <%= link_to 'Sign up', new_registration_path(resource_name), class: ''  %></div>
          <% end -%>

          <% end %>           

          <div class="row">
            <div class="col-md-12">
              <hr>

              <%- if devise_mapping.omniauthable? %>
              <%= link_to "Sign In with Facebook", user_facebook_omniauth_authorize_path(), class: 'facebookauth btn btn-primary col-md-4 col-md-offset-4', style: "margin-bottom: 20px" %>
              <% end -%>

              <%- if devise_mapping.omniauthable? %>
              <%= link_to "Sign In with GitHub", user_github_omniauth_authorize_path(), class: 'githubauth btn btn-default col-md-4 col-md-offset-4', style: "margin-bottom: 20px"%>
              <% end -%>

              <%- if devise_mapping.omniauthable? %>
              <%= link_to "Sign In with LinkedIn", user_linkedin_omniauth_authorize_path(), class: 'linkedinauth btn btn-info col-md-4 col-md-offset-4', style: "margin-bottom: 20px" %>
              <% end -%>
            </div>
          </div>

        </div>

      </div><!-- /.modal-content -->
    </div><!-- /.modal-dialog -->
</div><!-- /.modal compose message -->